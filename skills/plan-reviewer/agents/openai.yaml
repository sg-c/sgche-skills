interface:
  display_name: "Plan Reviewer"
  short_description: "Review and strengthen a committed plan twice"
  default_prompt: "Use $plan-reviewer to review and revise this committed Markdown plan twice."
